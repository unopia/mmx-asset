import sys
'''
import argparse


parser = argparse.ArgumentParser(argparse.SUPPRESS)
parser.add_argument("--a", required=True)
parser.add_argument("--e", required=True)
parser.add_argument("--c", type=str, required=True)
parser.add_argument("--integer_parameter", required=True)
FLAGS = parser.parse_args()
'''
print ("###################### input start ")
#print ('a : {} / e : {} / c : {} / integer_parameter : {}'.format(FLAGS.a, FLAGS.e, FLAGS.c, FLAGS.integer_parameter))
print (sys.argv)
print ("###################### input end")
